---
const baseFeatures = [
  "Estructuración completa de campañas",
  "1–2 campañas activas",
  "Optimización semanal de pujas y negativos",
  "Diseño y testeo de anuncios",
  "Configuración de conversiones",
  "Reporte mensual claro",
  "Acceso directo con tu gestor",
];

const crecimientoExtras = [
  "Mayor estructura de cuenta",
  "Más grupos y variantes de anuncios",
  "Testing continuo A/B",
  "Optimización avanzada de presupuesto",
  "Seguimiento estratégico mensual",
];
---

<section class="pricing" id="planes">
  <div class="pricing__container">
    <div class="pricing__header reveal">
      <span class="section-label">Inversión</span>
      <h2 class="pricing__title">
        <span class="line-reveal"><span>Planes con estructura</span></span>
        <span class="line-reveal line-reveal-d1"
          ><span><span class="keyword">según tu escala.</span></span></span
        >
      </h2>
      <p class="pricing__subtitle">
        Gestión profesional de Google Ads definida por nivel de inversión publicitaria.
      </p>
    </div>

    <div class="pricing__grid">
      <article class="pricing-card reveal reveal-delay-1" aria-label="Plan Base">
        <div class="pricing-card__top">
          <p class="pricing-card__name">Plan Base</p>
          <p class="pricing-card__range">
            Para negocios que invierten hasta $300.000 CLP mensuales en Google Ads.
          </p>
        </div>

        <div class="pricing-card__prices">
          <p class="pricing-card__price">$59.000 + IVA <span>/ mes</span></p>
          <p class="pricing-card__setup">Setup inicial (una vez): $79.000 + IVA</p>
        </div>

        <ul class="pricing-card__list">
          {
            baseFeatures.map((feature) => (
              <li>{feature}</li>
            ))
          }
        </ul>
      </article>

      <article
        class="pricing-card pricing-card--recommended reveal reveal-delay-2"
        aria-label="Plan Crecimiento"
      >
        <div class="pricing-card__badge">Recomendado</div>

        <div class="pricing-card__top">
          <p class="pricing-card__name">Plan Crecimiento</p>
          <p class="pricing-card__range">
            Para negocios que invierten entre $300.000 y $1.000.000 CLP mensuales.
          </p>
        </div>

        <div class="pricing-card__prices">
          <p class="pricing-card__price">$89.000 + IVA <span>/ mes</span></p>
          <p class="pricing-card__setup">Setup inicial: $99.000 + IVA</p>
        </div>

        <p class="pricing-card__note">Incluye todo lo anterior más:</p>
        <ul class="pricing-card__list">
          {
            crecimientoExtras.map((feature) => (
              <li>{feature}</li>
            ))
          }
        </ul>
      </article>

      <article class="pricing-card reveal reveal-delay-3" aria-label="Plan Escala">
        <div class="pricing-card__top">
          <p class="pricing-card__name">Plan Escala</p>
          <p class="pricing-card__range">
            Para inversiones superiores a $1.000.000 CLP mensuales.
          </p>
        </div>

        <p class="pricing-card__scale-copy">
          Estructuras avanzadas, automatización estratégica y gestión personalizada.
        </p>

        <a class="pricing-card__cta btn-secondary" href="#contacto">
          Solicitar evaluación personalizada
        </a>
      </article>
    </div>

    <div class="pricing__footer reveal reveal-delay-4">
      <p>Sin contrato de permanencia. Cancelas cuando quieras.</p>
      <p>
        La inversión en Google Ads se paga directamente a Google. Nosotros
        gestionamos la estrategia.
      </p>
    </div>
  </div>
</section>

<style>
  .pricing {
    background: var(--white);
    padding: var(--section-py) 0;
  }

  .pricing__container {
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 var(--section-px);
  }

  .pricing__header {
    margin-bottom: 3.5rem;
    max-width: 42rem;
  }

  .pricing__title {
    font-size: clamp(2.3rem, 5vw, 4rem);
    font-weight: 700;
    color: var(--black);
    line-height: 1.05;
  }

  .pricing__subtitle {
    margin-top: 1rem;
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--gray-500);
  }

  .pricing__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  @media (min-width: 940px) {
    .pricing__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      align-items: stretch;
    }
  }

  .pricing-card {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 1.75rem;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-lg);
    background: var(--off-white);
  }

  .pricing-card--recommended {
    background: var(--white);
    border-color: var(--black);
    box-shadow: 0 12px 34px rgba(10, 10, 10, 0.06);
  }

  .pricing-card__badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-weight: 600;
    color: var(--black);
    background: var(--gray-100);
    border: 1px solid var(--gray-200);
    border-radius: 999px;
    padding: 0.35rem 0.6rem;
  }

  .pricing-card__top {
    display: grid;
    gap: 0.75rem;
    padding-right: 5.8rem;
  }

  .pricing-card__name {
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--black);
  }

  .pricing-card__range {
    font-size: 0.9rem;
    line-height: 1.65;
    color: var(--gray-500);
  }

  .pricing-card__prices {
    display: grid;
    gap: 0.35rem;
    padding-top: 1.1rem;
    border-top: 1px solid var(--gray-200);
  }

  .pricing-card__price {
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 3.2vw, 2.2rem);
    line-height: 1.08;
    letter-spacing: -0.03em;
    color: var(--black);
    font-weight: 700;
  }

  .pricing-card__price span {
    font-family: var(--font-body);
    font-size: 0.85rem;
    letter-spacing: 0;
    color: var(--gray-500);
    font-weight: 500;
  }

  .pricing-card__setup {
    font-size: 0.875rem;
    line-height: 1.6;
    color: var(--gray-500);
  }

  .pricing-card__note {
    font-size: 0.8125rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
    color: var(--gray-500);
    margin-top: -0.15rem;
  }

  .pricing-card__list {
    display: grid;
    gap: 0.65rem;
  }

  .pricing-card__list li {
    position: relative;
    padding-left: 1.1rem;
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--gray-600);
  }

  .pricing-card__list li::before {
    content: "";
    position: absolute;
    top: 0.62em;
    left: 0;
    width: 0.32rem;
    height: 0.32rem;
    border-radius: 50%;
    background: var(--gray-300);
  }

  .pricing-card__scale-copy {
    font-size: 0.95rem;
    line-height: 1.75;
    color: var(--gray-500);
    margin-top: 0.25rem;
  }

  .pricing-card__cta {
    margin-top: auto;
    justify-content: center;
    width: 100%;
    border-color: var(--gray-300);
    background: var(--white);
  }

  .pricing-card__cta:hover {
    border-color: var(--black);
    background: var(--white);
  }

  .pricing__footer {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--gray-200);
    display: grid;
    gap: 0.5rem;
  }

  .pricing__footer p {
    font-size: 0.875rem;
    line-height: 1.65;
    color: var(--gray-500);
  }
</style>
