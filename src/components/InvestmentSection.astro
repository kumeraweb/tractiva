---
import { Check, Minus, Globe } from "@lucide/astro";

const includes = [
  "Estructuración completa de campañas",
  "Optimización semanal de pujas y negativos",
  "Diseño y testeo de anuncios",
  "Seguimiento de conversiones (llamadas + formularios)",
  "Reportes mensuales claros",
  "Acceso directo con tu gestor",
];
const excludes = [
  "Inversión en Google Ads (se paga directo a Google)",
  "Compra de dominio (gestionable por nosotros si lo necesitas)",
];
---

<section class="pricing" id="inversion">
  <div class="pricing__container">
    <div class="pricing__header reveal">
      <span class="section-label">Inversión</span>
      <h2 class="pricing__title">
        <span class="line-reveal"><span>¿Cuánto</span></span>
        <span class="line-reveal line-reveal-d1"
          ><span><span class="keyword">cuesta?</span></span></span
        >
      </h2>
      <p class="pricing__subtitle">
        Transparencia total. Sin sorpresas, sin cargos ocultos.
      </p>
    </div>

    <div class="pricing__body">
      <div class="pricing__prices reveal">
        <div class="pricing__price-block">
          <span class="pricing__label">Setup inicial (una vez)</span>
          <span class="pricing__value"
            >$75.000<span class="pricing__currency"> CLP + IVA</span></span
          >
        </div>
        <div class="pricing__divider"></div>
        <div class="pricing__price-block">
          <span class="pricing__label">Gestión mensual</span>
          <span class="pricing__value"
            >$50.000<span class="pricing__currency">
              CLP + IVA / mes</span
            ></span
          >
        </div>
      </div>

      <!-- Web incluida -->
      <div class="pricing__web reveal reveal-delay-1">
        <div class="pricing__web-icon">
          <Globe size={20} />
        </div>
        <div class="pricing__web-content">
          <h4 class="pricing__web-title">¿No tienes página web?</h4>
          <p class="pricing__web-text">
            Te la creamos incluida en el setup inicial. Recibes una landing page
            profesional lista para recibir tus campañas, con hosting básico
            gratuito.<br />
            <span class="pricing__web-note">
              Solo necesitas un dominio — si no tienes uno, te ayudamos a
              comprarlo.
            </span>
          </p>
        </div>
      </div>

      <div class="pricing__lists">
        <div class="pricing__list reveal reveal-delay-2">
          <h4 class="pricing__list-title">Incluye</h4>
          <ul>
            {
              includes.map((item) => (
                <li class="pricing__list-item">
                  <span class="pricing__icon pricing__icon--check">
                    <Check size={13} />
                  </span>
                  {item}
                </li>
              ))
            }
          </ul>
        </div>
        <div class="pricing__list reveal reveal-delay-3">
          <h4 class="pricing__list-title pricing__list-title--muted">
            No incluye
          </h4>
          <ul>
            {
              excludes.map((item) => (
                <li class="pricing__list-item pricing__list-item--muted">
                  <span class="pricing__icon pricing__icon--x">
                    <Minus size={13} />
                  </span>
                  {item}
                </li>
              ))
            }
          </ul>
        </div>
      </div>

      <div class="pricing__trust reveal reveal-delay-4">
        <p>
          Sin contrato de permanencia — cancela cuando quieras, sin
          penalización.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .pricing {
    background: var(--white);
    padding: var(--section-py) 0;
  }
  .pricing__container {
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 var(--section-px);
  }
  .pricing__header {
    margin-bottom: 3.5rem;
    max-width: 36rem;
  }
  .pricing__title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    color: var(--black);
    line-height: 1.05;
  }
  .pricing__subtitle {
    margin-top: 1rem;
    font-size: 1.0625rem;
    color: var(--gray-500);
  }

  .pricing__body {
    max-width: 48rem;
  }

  .pricing__prices {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid var(--gray-200);
    margin-bottom: 0;
  }
  @media (min-width: 640px) {
    .pricing__prices {
      flex-direction: row;
      align-items: flex-end;
      gap: 4rem;
    }
  }

  .pricing__price-block {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }
  .pricing__label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gray-400);
  }
  .pricing__value {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--black);
  }
  .pricing__currency {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray-400);
    letter-spacing: 0;
  }
  .pricing__divider {
    display: none;
    width: 1px;
    height: 4rem;
    background: var(--gray-200);
  }
  @media (min-width: 640px) {
    .pricing__divider {
      display: block;
    }
  }

  /* Web incluida block */
  .pricing__web {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    padding: 1.5rem;
    margin: 2.5rem 0;
    border: 1px solid var(--accent);
    border-radius: var(--radius-md);
    background: rgba(22, 163, 74, 0.04);
  }
  .pricing__web-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 50%;
    background: var(--accent-soft);
    color: var(--accent);
    flex-shrink: 0;
    margin-top: 0.1rem;
  }
  .pricing__web-content {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }
  .pricing__web-title {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 700;
    color: var(--black);
  }
  .pricing__web-text {
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--gray-500);
  }
  .pricing__web-note {
    font-size: 0.8125rem;
    color: var(--gray-400);
    font-style: italic;
  }

  .pricing__lists {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    padding-top: 2.5rem;
    border-top: 1px solid var(--gray-200);
    margin-bottom: 3rem;
  }
  @media (min-width: 640px) {
    .pricing__lists {
      grid-template-columns: 1fr 1fr;
    }
  }

  .pricing__list-title {
    font-family: var(--font-display);
    font-size: 0.8125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--black);
    margin-bottom: 1.25rem;
  }
  .pricing__list-title--muted {
    color: var(--gray-400);
  }

  .pricing__list-item {
    display: flex;
    align-items: flex-start;
    gap: 0.625rem;
    font-size: 0.9375rem;
    line-height: 1.5;
    color: var(--gray-500);
    padding: 0.375rem 0;
  }
  .pricing__list-item--muted {
    opacity: 0.6;
  }

  .pricing__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.25rem;
    height: 1.25rem;
    border-radius: 50%;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }
  .pricing__icon--check {
    background: var(--accent-soft);
    color: var(--accent);
  }
  .pricing__icon--x {
    background: var(--gray-100);
    color: var(--gray-400);
  }

  .pricing__trust {
    padding: 1.25rem 1.5rem;
    border-radius: var(--radius-md);
    border: 1px solid var(--gray-200);
    background: var(--off-white);
  }
  .pricing__trust p {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--gray-500);
  }
</style>
